[build]
  command = "npm rebuild better-sqlite3 --build-from-source && npm run seed && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[functions]
  included_files = ["data/**"]

[[plugins]]
  package = "@netlify/plugin-nextjs"
